<%- include('partials/head'); -%>
<h2>test</h2>

<section>
    <% projects.forEach(project => { %>
        <article>
            <h2><%= project.node.name %></h2>
            <p><%= project.node.description %></p>
            <p><%= project.node.pushedAt %></p>
           <p> <%= project.node.isPrivate %></p> 
        </article>
   <% }) %>
</section>
<%- include('partials/footer'); -%>